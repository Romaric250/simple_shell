/* create the atoi function and updae it in the builtins.c
 * file under the exit_cmd function.
 * dont forget to add the function to the shell.h file.
 * help fix betty syntax.
 * after writng the and updating the function to _atoi or somethingelse. run the program and see if it works correctly.
 * inlude your name in the Authors file.
